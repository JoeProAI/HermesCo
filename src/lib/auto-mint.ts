/**
 * auto-mint.ts — intentionally stubbed
 *
 * Soul minting is agent-driven (intentional), not automatic on boot.
 * Arweave writes cost money and should be deliberate.
 *
 * To mint: use the /soul page or POST /api/agent/salvage directly.
 */

// eslint-disable-next-line @typescript-eslint/no-unused-vars
export async function autoMintIfNeeded(_agentId: string): Promise<void> {
  // No-op: automatic minting disabled by design
}
